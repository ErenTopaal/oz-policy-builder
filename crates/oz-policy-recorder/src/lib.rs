//! Stellar transaction recorder.
//!
//! Phase 1 scope: skeleton only. The real `record_by_hash` and
//! `record_by_simulation` entrypoints land in P1-T3 (see `plan.md` § "Phase 1
//! — Foundations").

#![forbid(unsafe_code)]

pub mod placeholder;

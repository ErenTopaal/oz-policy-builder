//! Install-envelope builder.
//!
//! Phase 1 scope: skeleton only. The real `build_install_envelope`
//! implementation lands in Phase 2 (see `plan.md` § "Phase 2 — Policy IR &
//! Track A synthesizer").

#![forbid(unsafe_code)]

pub mod placeholder;

# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# Worktrees (managed by harness)
.claude/worktrees/
.worktrees/

# Node
node_modules/
dist/
*.tsbuildinfo
.pnpm-store/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Local env / secrets
.env
.env.local
*.local

# Logs and tmp
*.log
tmp/

# Reproducible-build manifests are per-release artifacts (uploaded to the
# GitHub release, not committed). See scripts/reproducible-build.sh and
# .github/workflows/reproducible-build.yml.
reproducible-build-manifest-*.json
# Docker-side cargo/target caches used by the reproducible-build workflow
# when invoked locally with the recipe in docs/reproducible-build.md.
.docker-cargo-home/
.docker-target/

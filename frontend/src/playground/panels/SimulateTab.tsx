// placeholder slot — wave 2 will populate with the permit row + deny matrix
// + re-simulate button. explicit empty marker, not mock data.

export function SimulateTab() {
  return <div style={{ padding: 24, color: "#a0a0a8" }}>Simulate — coming in wave 2</div>;
}

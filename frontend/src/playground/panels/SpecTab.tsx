// placeholder slot — wave 2 will populate with the IR tree + reasoning trace.
// this is NOT mock data: it is an explicit "empty slot" marker so honest
// failure is impossible to confuse with rendered output.

export function SpecTab() {
  return <div style={{ padding: 24, color: "#a0a0a8" }}>Spec — coming in wave 2</div>;
}

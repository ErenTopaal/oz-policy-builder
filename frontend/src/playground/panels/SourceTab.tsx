// placeholder slot — wave 2 will populate with the Monaco editor and the
// read-only Cargo.toml view. explicit empty marker, not mock data.

export function SourceTab() {
  return <div style={{ padding: 24, color: "#a0a0a8" }}>Source — coming in wave 2</div>;
}

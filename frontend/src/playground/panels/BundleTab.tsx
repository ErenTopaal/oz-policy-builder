// placeholder slot — wave 2 will populate with the JSZip-driven bundle
// download + install snippet. explicit empty marker, not mock data.

export function BundleTab() {
  return <div style={{ padding: 24, color: "#a0a0a8" }}>Bundle — coming in wave 2</div>;
}
